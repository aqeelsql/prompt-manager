server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /api/prompts/ {
        proxy_pass http://127.0.0.1:8000/prompts/;
    }

    location /api/reviews/ {
        proxy_pass http://127.0.0.1:8001/reviews/;
    }
}